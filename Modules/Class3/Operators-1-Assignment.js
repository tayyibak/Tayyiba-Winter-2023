/**
 * Assignment (=)
 *      is used to assign/reassign value in a variable.
 * 
 *      Js solves the right hand side of the assignment operator,
 *      then result gets assigned to the variable on left-hand side of assignment operator.
 * 
 */

 let planets = ['Earth', 'Mercury', 'Mars', 'Jupiter'];
 let userName = "Kiana Hisa";
 let userAge = 20;
 
 let result = 2 + 2;
 console.log(`result -> ${result}`);
 
 result = 11 - 21;
 console.log(`result -> ${result}`);